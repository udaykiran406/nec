apiVersion: v1
kind: Service
metadata:
  name: nec-middleware-service
  namespace: app
spec:
  type: NodePort
  selector:
    app: nec-middleware-service
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30084